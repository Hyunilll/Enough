<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.enough.dao.IEnoughDao">

	<select id="getproductList" resultType="com.enough.dto.ProductVO">
		select * from productList order by pseq desc
	</select>
	
	<insert id="insertpr" parameterType="com.enough.dto.ProductVO">
		INSERT INTO productList( pseq, name, brand, quantity, price)
		VALUES ( productList_seq.naexVal, #{name}, #{brand}, #{quantity}, #{price})
		
	</insert>
	
</mapper>